/**
 * EnumRecordStatus
 * Estados estándar de registro para auditoría y persistencia en el sistema.
 */
export enum EnumRecordStatus {
  ACTIVE = 'A',
  INACTIVE = 'I',
  DELETED = 'D',
  PENDING = 'P'
}
